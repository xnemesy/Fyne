# GoCardless (Nordigen) Sandbox Credentials
GOCARDLESS_SECRET_ID=SANDBOX_ID_HERE
GOCARDLESS_SECRET_KEY=SANDBOX_KEY_HERE

# Server Master Key (used only for server-side non-zero-knowledge fields if any, 
# but mostly client-side encryption is used now)
ENCRYPTION_MASTER_KEY=4e532f6b642f5... (64 hex characters)

# DB Config
DB_HOST=10.80.0.3
DB_USER=postgres
DB_PASSWORD=fyne-secure-pass-2026
DB_NAME=postgres
